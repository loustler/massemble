package io.loustler.massemble.heap

import org.scalatest.flatspec.AnyFlatSpecLike

final class HeapTest extends AnyFlatSpecLike {
  "Heap" should "be succeed" in {
    println("Must implements...")
  }
}